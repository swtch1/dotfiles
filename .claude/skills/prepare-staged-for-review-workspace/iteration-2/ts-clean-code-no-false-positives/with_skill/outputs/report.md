## Refactoring Changes Made

- `tests/notification-service.test.ts:8-16` — Added minimal ambient test-global type declarations (`describe`, `it`, `expect`) plus a small matcher type to remove TypeScript/LSP errors in this workspace without changing runtime behavior.

## Issues Found (Require Behavior Changes)

None.
