{
  "eval_id": 3,
  "eval_name": "add-validation-legacy-handler",
  "prompt": "We need input validation on our user registration handler. Currently it accepts anything and lets the DB complain. Add validation: email must be valid format, name can't be empty or over 100 chars, password must be at least 8 chars. The handler is in handler.go. There are no existing tests for this handler — you'll need to add them. The handler depends on db.UserStore which is an interface (see db.go for the shape).",
  "assertions": [
    "Characterization tests for existing handler behavior are written first (valid request succeeds, invalid JSON returns 400)",
    "A test list or plan is created before writing validation tests",
    "Validation tests are written BEFORE validation code is added to the handler",
    "A fake or stub UserStore is created for testing (not a mock framework)",
    "Tests cover: invalid email format, empty name, name over 100 chars, password under 8 chars",
    "Tests verify the HTTP status code and error message for each validation failure",
    "Valid requests still succeed after validation is added",
    "Each test tests one validation rule (not a single test checking all validations)",
    "Validation logic is clean — not deeply nested if/else chains"
  ]
}
