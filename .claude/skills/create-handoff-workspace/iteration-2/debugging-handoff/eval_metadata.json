{
  "eval_id": 2,
  "eval_name": "debugging-handoff",
  "prompt": "Create a handoff for flaky TestConcurrentOrderProcessing debugging — root cause found, fix pending",
  "assertions": [
    "A handoff markdown file was created",
    "The handoff document is under 100 lines total",
    "The YAML frontmatter has 7 or fewer fields",
    "The Learnings section explains the deadlock root cause inline (Dispatch RLock + handler Lock) rather than just referencing debug-notes.md",
    "The References section has 3 or fewer files in the 'read on resume' tier",
    "The document does NOT have a separate 'Recent changes' or 'Artifacts' section",
    "There is a single clear 'Next Step' direction rather than a numbered list of 5+ items",
    "The next step recommends a specific fix approach rather than just 'continue debugging'",
    "The document does not contain code blocks longer than 10 lines",
    "The response includes /resume-handoff command"
  ]
}
